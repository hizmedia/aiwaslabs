# Copy this to .env.local in storefront/ and admin/ — never commit the real values
DATABASE_URL=postgresql://USER:PASSWORD@HOST/neondb?sslmode=verify-full
JWT_SECRET=replace-with-a-long-random-string
